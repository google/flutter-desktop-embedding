name: custom_cursor
description: An implementation of how to change the cursor at runtime on desktop
version: 1.0.0
author:
homepage:

flutter:
  plugin:
    platforms:
      # linux:
      #   pluginClass: CustomCursor
      macos:
        pluginClass: CustomCursor
      windows:
        pluginClass: CustomCursor

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
